interface:
  display_name: "Agent Creator"
  short_description: "Mandatory hub-template worker provisioning"
  default_prompt: "Use $agent-creator before any new worker create. Hub list, hub get, then participant create --type agent --bind create --from-template with --env."
